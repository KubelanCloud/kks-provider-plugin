apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kloud-csi.fullname" . }}-driver
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kloud-csi.labels" . | nindent 4 }}
data:
  driver.hcl: |
    driver {}
    client {}
